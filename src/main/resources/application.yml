# ===================================================================
# CONFIGURAÇÕES COMPARTILHADAS (TODOS OS PERFIS)
# ===================================================================
server:
  servlet:
    context-path: /

spring:
  application:
    name: agendei

  jackson:
    default-property-inclusion: non_null
    time-zone: America/Sao_Paulo

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update  # seguro para produção

  mail:
    host: smtp.gmail.com
    port: 587
    username: agendeiofficial@gmail.com
    password: ygrcnbgqhkvxxypj
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  maps:
    key: AIzaSyDQHCfEBOf_EO6Abo4Q-n987llQhru87Rw

  jwt:
    SECRET_KEY: chave-super-secreta-de-256-bits-jwt-agendei

  profiles:
    active: sqlserver   # dev | localmysql

---
# ===================================================================
# PERFIL SQL SERVER
# ===================================================================
spring:
  config:
    activate:
      on-profile: sqlserver

  datasource:
    url: jdbc:sqlserver://agendeidb.database.windows.net:1433;databaseName=agendeidb;encrypt=true;trustServerCertificate=false;loginTimeout=30;
    username: dbmaster
    password: Agendei@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

    hikari:
      minimum-idle: 0
      maximum-pool-size: 5
      idle-timeout: 300000
      max-lifetime: 600000
      connection-timeout: 30000
      pool-name: AgendeiHikariPoolSQLServer

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect

server:
  address: 0.0.0.0
  port: 8080

---
# ===================================================================
# PERFIL DEV — H2 (LOCAL)
# ===================================================================
spring:
  config:
    activate:
      on-profile: dev
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:agendei
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect


---
# ===================================================================
# PERFIL PROD — MYSQL ORACLE CLOUD (VCN PRIVADA)
# ===================================================================
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://10.0.2.21:3306/agendei?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: agendei_root
    password: Agendei@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  h2:
    console:
      enabled: false


---
# ===================================================================
# PERFIL LOCALMYSQL — MYSQL LOCALHOST
# ===================================================================
spring:
  config:
    activate:
      on-profile: localmysql
  datasource:
    url: jdbc:mysql://localhost:3306/agendei?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  h2:
    console:
      enabled: false

